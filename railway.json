{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf dist server-dist && vite build && esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outfile=server-dist/index.mjs"
  },
  "deploy": {
    "startCommand": "NODE_ENV=production node server-dist/index.mjs",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
