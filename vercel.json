{
  "buildCommand": "npx tsx scripts/prerender-blog.ts && npx tsx scripts/prerender-pages.ts && npx vite build && npx tsx scripts/generate-seo-pages.ts",
  "outputDirectory": "dist/public",
  "framework": "vite",
  "installCommand": "npm install --include=dev",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
